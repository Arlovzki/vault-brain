{
  "name": "vault-brain-mcp-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Remote MCP server for a Markdown vault stored in Amazon S3.",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "node build.mjs"
  },
  "dependencies": {
    "@aws-sdk/client-cognito-identity-provider": "^3.0.0",
    "@aws-sdk/client-s3": "^3.0.0",
    "@aws-sdk/credential-provider-ini": "^3.973.15",
    "aws-jwt-verify": "^4.0.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.0",
    "@types/node": "^22.0.0",
    "esbuild": "^0.24.0",
    "typescript": "^5.6.0"
  }
}
